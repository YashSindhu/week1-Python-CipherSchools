# This means we cann't change the string once we created it
string = "string"
print(string)
# it gives --> string
print(string.replace('t','T'))
# it gives --> sTring   , it doesn'r change the original string it just create a new one.
